[project]
name = "JOSIF-Linkage"
version = "1.0.0"
description = ""
authors = []
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas (>=2.2.3,<3.0.0)",
    "recordlinkage (>=0.16,<0.17)",
    "fastparquet (>=2026.3.0,<2027.0.0)"

]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
packages = [{include = "linkage"}]